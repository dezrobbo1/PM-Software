# Archived project-control material

This directory contains files that previously sat at the repository root or under active GitHub configuration paths.

They are retained for history and reference, but they no longer control proof-of-concept development. In particular, the archived workflows, repository-governance profile and SHA-256 manifest are not active CI or merge requirements.
