# Phase 0 Amendment `phase0-0.1.4` — Executable-Claim and Oracle Hardening

Date: 17 August 2026
Trigger: fresh Codex review of PR #1
Results existing when proposed: **none**

Classes: semantic-profile binding, schema revision, benchmark identity freeze and validation.

## Changes

- Added independent machine evaluation of every declared relationship formula over the expected activity coordinates, using signed productive lag on the successor calendar and the declared actual/status policy.
- Froze the exact set of 50 `case_id` values and their canonical filenames; count and catalogue agreement alone are no longer sufficient.
- Preserved `reference-v0.1` and `reference-v0.2`, created active `reference-v0.3`, and removed executable claims for explicit alternate lag calendars and cumulative capacity because the frozen corpus has no direct fixture for either semantic.
- Required a supplied approved forecast to exactly cover the schedule activity set so approved-forecast movement cannot silently use a zero substitute for omitted activities.
- Advanced the execution-record schema to `0.1.4`; an `infeasible_proven` record must carry no selected-scenario hash, an empty objective vector and null best-bound/gap values.
- Added focused negative guards for each review finding.
- Independently recomputed the exact canonical coordinates for all 49 declared fixtures, including productive duration, supported date bounds, status/actual policy and canonical earliest placement.
- Checked productive half-open capacity-one exclusive-resource usage and independently selected the frozen DET-049/050 resource order from the complete objective vector.
- Recomputed every objective-vector entry from complete feasible selected states for proposed scenarios, execution evidence and feasible patched counterfactuals; missing state evidence now fails closed while non-optimisation runs remain unaffected.
- Bound external execution/explanation states to the claimed evidence hash, rejected approved-forecast counterfactual patches, and validated each patched canonical input before accepting a feasible counterfactual vector.
- Required complete, recomputed total/free float values in the two restricted float fixtures.
- Froze the existing curated `driving_relationships` assertion sets and verified that each listed relationship actually attains the calendar-adjusted governing coordinate without claiming an exhaustive critical-path set.
- Added eight focused guards for the follow-up review, bringing the complete guard suite to 67 tests.

## Evidence and impact

All 50 case identities, activity/resource/calendar inputs, relationship types and lags, and declared expected calculation values remain unchanged. Only their active semantic-profile reference changed from v0.2 to the deliberately narrower v0.3. The canonical schema remains `0.1.3`; no model expansion was needed for these validator-only guards. No CPM, optimisation, native-product, practitioner or buyer result existed when this amendment was made.
