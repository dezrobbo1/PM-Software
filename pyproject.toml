[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "deterministic-scheduling-core-phase0"
version = "0.1.3"
description = "Frozen experiment design and semantic fixtures for a deterministic scheduling-core benchmark"
requires-python = ">=3.11"
dependencies = [
  "jsonschema==4.26.0",
  "rfc3339-validator==0.1.4"
]

[tool.setuptools]
packages = []

[tool.dsc]
phase = "0"
status = "protocol-frozen"
implementation_started = false
